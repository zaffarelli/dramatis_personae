
module my-logrotate 1.0;

require {
	type unlabeled_t;
	type logrotate_t;
	class file { create getattr lock open read rename setattr unlink write };
}

#============= logrotate_t ==============
allow logrotate_t unlabeled_t:file { create getattr lock open read rename setattr unlink write };
